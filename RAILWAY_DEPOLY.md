[Uploading RAILWAY_DEPLOY.md…]()
# 🚀 دليل نشر البوت على Railway

## الخطوات بالترتيب

---

### الخطوة 1 — ارفع الملفات على GitHub

أنشئ مستودع جديد على GitHub وارفع هذه الملفات:
```
multilingual_bot.py
requirements.txt
Procfile
runtime.txt
.gitignore
```
> ⚠️ لا ترفع `config.json` أو `vpn_shop.db` — المعلومات الحساسة تُضاف في Railway مباشرة.

---

### الخطوة 2 — أنشئ مشروعاً على Railway

1. افتح [railway.app](https://railway.app) وسجّل دخولك
2. اضغط **New Project**
3. اختر **Deploy from GitHub repo**
4. اختر المستودع الذي رفعته

---

### الخطوة 3 — أضف متغيرات البيئة (Variables)

في لوحة Railway اضغط على المشروع ثم **Variables** وأضف:

| المتغير | القيمة | ملاحظة |
|---------|--------|---------|
| `BOT_TOKEN` | `8529629429:AAGBa15p...` | توكن البوت من @BotFather |
| `ADMIN_IDS` | `2005350858` | يد التيليغرام الخاصة بك (فاصل بينها بفاصلة لو أكثر من واحد) |
| `SUPPORT_USERNAME` | `@NBM_1` | يوزر الدعم |
| `TRIAL_DAYS` | `3` | أيام التجربة المجانية |
| `REFERRED_TRIAL_DAYS` | `7` | أيام التجربة عند الإحالة |
| `PAYMENT_PROVIDER_TOKEN` | *(اتركه فارغاً)* | لـ Telegram Stars لا تحتاج قيمة |
| `WEBHOOK_URL` | *(اتركه فارغاً الآن)* | سنعبئه في الخطوة 4 |

---

### الخطوة 4 — احصل على رابط Railway وأضفه

1. بعد أول Deploy، اضغط على **Settings** ثم **Domains**
2. اضغط **Generate Domain** — ستحصل على رابط مثل:
   ```
   https://vpn-bot-production.up.railway.app
   ```
3. ارجع إلى **Variables** وأضف:
   ```
   WEBHOOK_URL = https://vpn-bot-production.up.railway.app
   ```
4. Railway سيُعيد تشغيل البوت تلقائياً بـ Webhook ✅

---

### الخطوة 5 — تحقق من أن البوت يعمل

افتح Telegram وأرسل `/start` للبوت.
إذا رد — كل شيء تمام! 🎉

---

## ⚠️ تحذير مهم: قاعدة البيانات

Railway **لا تحفظ الملفات** بين عمليات إعادة التشغيل.
هذا يعني أن `vpn_shop.db` ستُحذف عند كل Redeploy.

### الحل المؤقت (للتجربة):
البوت يعمل عادياً لكن البيانات تُفقد عند إعادة التشغيل.

### الحل الدائم (للإنتاج):
استخدم قاعدة بيانات خارجية. أسهل خيار مجاني هو **Railway PostgreSQL**:

1. في Railway اضغط **New** → **Database** → **PostgreSQL**
2. أضف متغير `DATABASE_URL` الذي سيظهر تلقائياً
3. عدّل الكود ليستخدم PostgreSQL بدل SQLite

*(راجعني إذا أردت هذا التعديل)*

---

## 🔄 تحديث البوت

لتحديث البوت فقط:
```bash
git add .
git commit -m "تحديث"
git push
```
Railway سيُعيد النشر تلقائياً ✅

---

## 🆘 استكشاف الأخطاء

| المشكلة | الحل |
|---------|------|
| البوت لا يرد | تحقق من `BOT_TOKEN` في Variables |
| خطأ عند بدء التشغيل | اضغط **Deployments** وشاهد اللوج |
| البوت يعمل محلياً لكن لا على Railway | تأكد أن `WEBHOOK_URL` مضبوط صح |
| البيانات تُفقد | استخدم PostgreSQL (انظر أعلاه) |
